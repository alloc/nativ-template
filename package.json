{
  "name": "my-nativ-app",
  "main": "expo-router/entry",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "expo start",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "web": "expo start --web",
    "android:clean": "expo prebuild -p android --clean",
    "ios:clean": "expo prebuild -p ios --clean"
  },
  "dependencies": {
    "nativ": "latest"
  },
  "devDependencies": {
    "@alloc/prettier-config": "latest",
    "@babel/core": "latest",
    "@types/node": "latest",
    "drizzle-kit": "beta",
    "prettier": "latest"
  },
  "prettier": "@alloc/prettier-config"
}
